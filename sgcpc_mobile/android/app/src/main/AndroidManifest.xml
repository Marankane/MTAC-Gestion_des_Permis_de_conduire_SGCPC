<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Réseau : appels API vers le serveur Django -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Caméra : photos du permis confisqué, carte grise, PV, dégâts... -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Localisation GPS optionnelle du lieu de l'accident (cf. CDCF Module 1) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="SGCPC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic=true : nécessaire uniquement si le serveur Django est
             en HTTP pur en développement/pilote (cf. CDCF section 6.1, déploiement pilote
             à Niamey). À retirer en production dès que le serveur est en HTTPS (TLS 1.3
             exigé par le CDCF section 4.3). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
